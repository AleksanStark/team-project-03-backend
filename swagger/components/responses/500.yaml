description: Something went wrong
content:
  application/json:
    schema:
      type: object
      required:
        - status
        - message
        - data
      properties:
        status:
          type: integer
          example: 500
        message:
          type: string
          example: Something went wrong! Try again!
        data:
          type: object
